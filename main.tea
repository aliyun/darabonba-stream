/**
 * This is a stream module
 */

static function readFromFilePath(path: string): readable;

static function readFromBytes(raw: bytes): readable;

static function readFromString(raw: string): readable;

static function reset(raw: readable): void;
